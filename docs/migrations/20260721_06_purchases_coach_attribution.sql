-- ============================================================
-- 20260721_06：銷售業績歸屬（銷售/儲值彈窗）
-- ------------------------------------------------------------
-- purchases 增加 coach_id（text，nullable）——「銷售/儲值」彈窗售出教練課/團體課時
-- 記錄業績歸屬教練。null＝無歸屬（商品/場租/重啟或改版前的舊紀錄）。
-- 前端 dbPutPurchaseSafe 在欄位不存在時會自動退回不帶 coach_id 重寫（收款不漏記），
-- 故本檔可晚於前端上線，但未套用前業績歸屬不會被記錄。
--
-- 套用狀態：測試庫已於 2026-07-21 套用。正式庫待使用者於 SQL Editor 執行。
-- 回退：alter table purchases drop column coach_id;
-- ============================================================
alter table purchases add column if not exists coach_id text;
