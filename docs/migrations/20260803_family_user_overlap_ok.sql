-- 家庭共享：自主訓練指定「使用人」時，可與本人的其他課程同時段
-- （2026-08-03 使用者案例：媽媽 11:00 上教練課，爸爸想用同一個帳號約 11:00 的自主訓練。
--   已套用正式庫，完整內容見 Supabase migration family_user_overlap_ok。）
--
-- 原本「同會員、時段重疊」的防呆只放行「兩邊都是自主訓練」——那是防同一個人分身。
-- 家庭共享帳號背後不只一個人：自主訓練具名了使用人（trial_name＝家庭成員稱呼），
-- 就不是本人在用這個時段，放行。沒指定使用人（＝本人）維持原防呆。
--
-- ・fn_member_self_book：p_family_user 有值 → 跳過「非自主訓練重疊」檢查
-- ・fn_member_self_reschedule：搬的那筆 trial_name 有值 → 同樣放行
-- ・前端 validateBooking 同一條規則（任一邊是家人用的自主訓練即放行），
--   櫃檯代訂家人使用時也一致
-- ・順帶移除 fn_member_self_book 的 5 參數舊版：兩個同名函式帶預設值，
--   不帶 p_units 的呼叫會撞名（PGRST203）
