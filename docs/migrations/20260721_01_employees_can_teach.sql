-- ============================================================
-- 20260721_01：員工「開放開課」開關（employees.can_teach）
-- ------------------------------------------------------------
-- 背景（2026-07-21 使用者指示）：不是每個員工都可以開課。
-- 員工設定新增「開放開課」開關；關閉後不出現在：
--   行事曆教練篩選 chips / 篩選下拉、新增預約教練名單、代課教練名單。
-- 報表與歷史資料（員工表現、教練分析）不受影響（僅過濾「選人」介面）。
-- 預設 true＝既有員工全部維持可開課。
--
-- 套用狀態：測試庫 kucvxpjatptfckhlxptj 已於 2026-07-21 套用。
-- ⚠️ 正式庫必須「先套本檔、再推前端」：前端 dbPut 會整筆 upsert，
--    欄位不存在時 PostgREST 會報 unknown column，員工資料將無法儲存。
-- 回退：alter table employees drop column can_teach;
-- ============================================================
alter table employees add column if not exists can_teach boolean not null default true;
