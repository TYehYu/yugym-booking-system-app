# YUGYM OS｜Sprint 2 RLS 四角色測試結果

> 產出：2026-07-16　環境：測試專案 `yugym-sprint3-test`（schema 與正式 1:1、無真實個資）
> 已套 `docs/migrations/20260716_03_rls_hardening.sql`。**正式資料庫未套用。**

---

## 0. 結論

RLS 收斂後，四角色隔離全部正確。**「未登入即讀全部個資」的漏洞已封閉。** 可作為正式套用的依據（正式套用仍需先做前端各頁回歸，見 §4）。

四個測試帳號：admin / fd / coach@staff.yugym.local、0900000001@member.yugym.local（綁 m_reg）。
**密碼另行私下保管，不寫入本 repo（repo 為公開）。**

---

## 1. 可見筆數矩陣（各角色登入後 GET，數字＝可讀筆數）

| 表 | anon | 會員 | 教練 | 櫃台 | 管理員 | 判定 |
|---|---:|---:|---:|---:|---:|---|
| members | 0 | 1 | 3 | 9 | 9 | ✅ 會員只自己、教練只學員、櫃台/管理員全部 |
| member_tickets | 0 | 5 | 14 | 14 | 14 | ✅ 會員只自己、員工可讀 |
| bookings | 0 | 7 | 16 | 16 | 16 | ✅ 會員只自己、員工可讀 |
| ticket_logs | 0 | 10 | 18 | 18 | 18 | ✅ 會員只自己票券異動 |
| salary_templates | 0 | 0 | 0 | 0 | 0* | ✅ 非管理員全擋（*測試未種薪資資料） |
| ticket_types | 0 | 6 | 6 | 6 | 6 | ✅ 登入者皆可讀（設定型）|

> anon 全 0＝未登入者讀不到任何營運資料，漏洞已封。

## 2. 寫入權限（實際影響筆數，0＝被 RLS 擋）

| 操作 | 會員 | 教練 | 櫃台 | 管理員 | 判定 |
|---|---:|---:|---:|---:|---|
| 改票種設定 ticket_types | 0 | 0 | 0 | 1 | ✅ 只有管理員可改設定 |
| 改會員資料 members | 0（改別人）| 0 | 1 | — | ✅ 只有櫃台/管理員可改 |
| 改自己授課的預約 bookings | — | 6 | — | — | ✅ 教練可改自己的 |
| 改別人授課的預約 bookings | — | 0 | — | — | ✅ 教練不能改別人的 |
| 會員改自己的預約 | 0 | — | — | — | ✅ 會員對預約唯讀 |

## 3. 對應的權限函式（皆 Sprint 1 既有，無需新增）

`is_admin` / `is_staff_desk` / `is_coach` / `is_any_staff` / `current_employee_id` / `current_member_id` / `can_coach_see_member`（教練只見學員即靠此）。

## 4. 正式套用前的注意（交棒）

1. **前端各頁回歸**：收斂後，教練 `dbGetAll('members')` 只回學員、`salary_templates` 回空等。需以四角色各走一遍前端主要頁面，確認沒有頁面因「讀不到原本全開的資料」而空白或報錯。這是正式套用的最後一道關卡。
2. **會員自我隔離政策已一併驗證**：正式庫會員目前 0 帳號，這些政策不影響現況；未來開放會員登入即自動生效。
3. **purchase_applications**：收斂為 anon 僅可送出(insert)、讀寫限櫃台。若線上購課需讓會員查自己申請狀態，未來加會員身分判斷。
4. 套用步驟：正式庫執行 `20260716_03_rls_hardening.sql` → 四角色前端回歸 → 有異狀用回滾（恢復該表全開）即可。

> 目前正式資料庫、正式網站、master 皆未變動。
