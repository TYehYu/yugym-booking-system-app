# YUGYM 交接說明（跨機器接續）

> 更新：2026-07-18 深夜（收工）。此檔隨 repo `git clone` 帶走，讓另一台電腦（或新的 Claude Code 對話）快速接手。
> **repo 為公開**，本檔不放任何密碼、不放漏洞細節。測試帳密與資安鑑識紀錄見專案根目錄
> `PRIVATE_DO_NOT_UPLOAD_security-incident-20260716.md`（受 `.gitignore` 保護，不進版控，只在本機）。

---

## 1. 現在的狀態（一句話）

Sprint 2/3 已上線穩定運作；7/17 完成首頁改版（Dashboard V2）；7/18 白天修行事曆競態、票券 V4 落差 1/2、
Inline Expand Card 階段 0~3b；7/18 晚間完成 **Calm Play 行事曆全面改版**（平塗形狀 Token、4 等寬 Lane、
周邊四快捷按鈕、團課名單簽到、代課快捷）＋ Dashboard V2.1（今日教練卡）＋ 會員升降級自動制 ＋
會員票券逐堂 Token。**線上版本 `260718.2345`**。白天詳情見 §1.4、晚間見 §1.4b。

## 1.4 今天（2026-07-18）做了什麼

**全部已上線，逐項瀏覽器實測（本機測試庫）後才推 master。**

1. **VIP／限定教練課結案**：使用者澄清這兩者就是一般教練課（活動價、已停售），非獨立方案、
   無資料模型遷移需求。`requires_coach` 已補為 `true`（正式庫，經同意）。詳見 §4 待辦 #1（已結）。
2. **票券 V4 落差分析**：新增 `docs/TICKET_SYSTEM_V4_GAP_ANALYSIS.md`。結論：V4 骨架大多已實作，
   剩 4 落差。**落差 3（分期）決策：保留現行彈性版**，規格 §4 固定切法不實作。
3. **行事曆「點空格偶爾帶不到日期」修復**（260718.0904）：根因為跟隨滑鼠的 [＋] 預覽卡用全域
   `_chpDate/_chpTime` 有競態。移除浮動 [＋]、改回 `.cal-half` 整列反白 + 格子自身 onclick（字面值、無競態）。
   top [＋] 改為時間軌最上緣的「營業前列」。過去日期不可點。
4. **自主訓練發點效期**：基準改為 `booking.date`（課程日，對齊規格 §3）；因補簽限當天，現行零行為差異。
5. **票券 V4 落差 1：方案複製**（260718.0909）：方案列表加「複製」→ `clonePlan`，以新增模式預填、名稱加「（複製）」。
6. **票券 V4 落差 2：方案封存三態**（260718.0924）：`course_plans` 加 `archived` 欄位
   （migration `docs/migrations/20260718_01_*.sql`，測試＋正式庫皆已套用）。封存 = active=false+archived=true，
   售票路徑皆依 active 過濾故自動排除；清單預設隱藏封存、可「顯示已封存」/「還原」。`archivePlan`/`unarchivePlan`/`togglePlansArchived`。
7. **Inline Expand Card 行事曆改版啟動**（規範 `docs/INLINE_EXPAND_CARD_SPEC.md`）：
   - 階段 0（260718.0924）：**週曆改為一次顯示整週 7 天**（原為 5 天視窗+滑動）。
   - 階段 1（260718.0942）：**卡片標準化** —— 內容只留時間+會員姓名；課程色上 20% 色帶 + 教練色下 80% 底；
     移除特殊卡差異；簽到改右上角 ✓ 角標。
   - 階段 2（260718.0953）：**Hover Fish-eye** —— 滑過卡片原地放大顯示完整資訊（時間區間/課程/教練/票券/狀態）。
   - 階段 3a（260718.1026）：**Click 原地展開** —— 點卡不開 Modal，原地展開查看 + 簽到/取消（卡內兩段確認）；
     靠邊卡片加**邊緣感知**（hover 與展開都會自動往內、不超出畫面）。
   - 階段 3b（260718.1042）：**卡內直接編輯** —— 展開卡「編輯」原地改日期/時間/時長/教室/代課教練，
     重用 `saveBookingTime`（含驗證），取代編輯 Modal。（現行實際可改欄位就這些）
   - **待做**：階段 4（新增預約也改原地展開，取代新增 Modal）。取代營運核心，逐階段做、保留舊流程可回退。

## 1.4b 今天晚間（2026-07-18 15:30~23:45，17 個版本）做了什麼

**設計輸入**：`YUGYM_OS_Calm_Play_Calendar_Design_V3.docx`（MUJI×Apple×Nintendo）、Soft UI 參考圖、
平塗形狀參考圖、`YUGYM_OS_手機版首頁更新計畫_V1.docx`（尚未實作，見待辦）。逐版與使用者迭代收斂。

1. **行事曆 Calm Play Token**（多輪迭代定案）：
   - Shape=課程（■教練課 ●友善 ▲體驗 ◆團體 ⬢自主；按摩併■）、Color=教練（**全教練換低彩度淡色系**，保留各自色相）。
   - Token=純平塗形狀（`.evc-body` clip-path 底層）＋文字層（`.evc-txt` 置中疊上、可超出形狀）。曾做過的粗描邊/白圈徽章/黑邊皆被使用者否決移除。
   - **4 等寬 Lane**（`assignLanesDay` 重寫）：特殊區（團課/跑步機/溢位）廢除，同時段最多 4 卡等寬並排；第 5 堂疊末 Lane 紅框防呆。
   - **互動定案**：hover 只微放大 106%（不展資訊卡）；點擊周邊滑出四按鈕 —— 左「代課」「取消」右「編輯」「簽到」。
     團課簽到開名單逐人點名（新舊資料結構皆支援）；代課開教練清單含衝突檢查（新功能）；
     取消/取消簽到皆兩段式按鈕確認（`undoCheckin` 的原生 confirm 已以 `undoCheckinDirect` 取代——原生對話框會凍結分頁）。
   - 篩選列：教練 chips 穿教練色、課程 chips 中性色＋形狀 glyph；圖例同步改形狀。
2. **友善課分類 bug 修復**：現行預約流程 booking 不帶 `ticket_type_id`，友善課判定從未生效、一直被當一般教練課。
   `evColorClass` 增加第 3 依據＝票券快照 `plan_name`（呼叫端帶入 ticketMap），課程篩選同步。
3. **Dashboard V2.1**：KPI「今日總課堂/剩餘課堂」→「教練課/團體課」；新增「**今日教練**」卡
   （每教練一列：色籤＋值班五態[請假/已下班含時數/值班中含已值時數/未打卡紅字/尚未上班]＋當日課程 mini Shape Token）。
   原時間軸暫留（與新卡內容重疊，待使用者定奪去留）。
4. **會員升降級自動制**（使用者定案規則）：連續 3 個完整月教練課（含友善）簽到 ≥4 堂→主顧客；主顧客連 2 個完整月未達→會員。
   `computeMemberTiers` 逐月重播狀態機、純推算不落庫；VIP 手動且凌駕。接入會員列表/詳情/搜尋/分析/主顧客方案購買資格
   （`memberCanUseLoyal`）。合成 6 情境測試全過。**注意：原手動標記 loyal 的會員將以自動判定為準。**
   **起點條款（260718.2355 追加，使用者指示）**：制度起點 `TIER_EPOCH_YM='2026-07'`，起點時**所有既有會員
   （含無簽到史者，走 `__default`）一律主顧客**，起點前歷史不追溯；之後未達標自然降級 ——
   第一批降級將出現在 **2026-09-01**（7、8 月兩個完整月皆 <4 堂者）。
5. **會員票券逐堂 Token**：`ticketTokens()` 取代圓點 —— N 堂=N 個課種形狀圖示，已用=淡底+上課日期、
   分期未開通=鎖、無紀錄的已用格=✓；快到期排最上、永久有效其次、失效淡化沉底（排序 bug 順手修：null 到期日原本排最前）。
6. 既有 bug 修復：展開卡邊緣感知只看視窗、忽略 `.cal-body` overflow 裁切（3a 起就有）→ 改量測最近裁切祖先＋上/左緣感知。

**驗證方式**：全程本機（測試庫）瀏覽器實測——簽到/取消簽到實際執行並還原、代課面板、團課名單、
課程篩選、Dashboard 切 7/17 檢查、票券 Token 合成資料渲染。中途兩次改壞（`_isSpecial` 殘留引用、
原生 confirm 凍結分頁）皆被實測抓到並修復。

**回退**：整包前端改版，`git revert` 對應 commit 即可（無 DB migration、無 RPC 變動）。

## 1.4c 深夜追加（260719.0015～0100，正式站 bug 回報後）

1. **🔴 重大修復：周邊四按鈕點了全沒反應**（正式站實際回報，葉政義 18:00 課簽到失敗）。
   根因：行事曆拖曳系統的 pointerdown 豁免名單（index.html `wrap.addEventListener('pointerdown')`）
   只有舊的 `.evc-actions, .evx-edit`，新的 `.evc-orbit` 按鈕沒在名單 → pointer capture 吞掉按鈕
   click、pointerup 又被當成點卡收合。已補 `.evc-orbit, .evc-roster`。
   **教訓＝豁免名單必須跟上新互動元件；測試務必用真實點擊（JS `btn.click()` 繞過 pointer 事件，
   當晚的「已實測」全是假陽性）。** 修復後簽到/取消簽到/編輯皆以真實點擊重新驗證通過。
2. checkInBooking 整包 try/catch＋友善錯誤 toast（禁止沉默失敗）；點擊縫隙誤收合加緩衝。
3. **卡片視覺定版（使用者最終指示）**：統一方形＋1px 細黑框、底色回歸課程色系
   （--course-*-soft 淡底＋accent 字）、第三行教練縮寫 tag（教練色，Randy→RA、代課標記）。
   形狀 Token 自行事曆退場；篩選 chips／首頁 mini token／會員票券 token 的形狀 glyph 保留。
4. 殘留掃描：tok-*／evc-pill／evc-shape／--tok-* 等歷次迭代殘留 0；console 乾淨；
   首頁/會員頁/行事曆三頁快掃正常。
5. 葉政義 IMP-00221（7/18 18:00）仍為 booked —— **待明日以「補簽」處理或使用者指示後台代簽**。

## 1.4d 今天（2026-07-19）做了什麼

1. **GPT 交接包整合**：使用者提供與 ChatGPT 討論的規劃包（角色工作區、薪酬規則、會員註冊、
   LINE 方向、UAT/SOP），已歸檔至 `docs/specs-20260719/`（**先讀該資料夾 README.md 的勘誤**——
   原文件的「已完成」清單多為虛報）。三支 migration 歸檔至 `docs/migrations/20260719_01~03_*.sql`，
   **已套用於測試庫、正式庫未動**（經逐項驗證）。
2. **三項決策定案**（詳見 specs README）：
   - 視覺＝Crextio 參考圖 × 品牌綠（淡黃改品牌綠；行事曆課卡維持 7/18 定版）。
   - 發點＝**補簽也發點**（依原課程日；課程日第 1 天、第 7 天到期）。正式庫套 `20260719_01` 生效，**待批准**。
   - 簽到＝**全面開放**（會員自簽＋教練代簽＋櫃台代簽）；會員自簽功能尚未實作。
3. **首頁視覺改版 V1**（260719.2210）：畫布米白→淡綠漸層、首頁卡片 24px 大圓角
   去硬框、右欄今日待辦改深綠深色卡、快速操作淡綠磚。樣式集中在 `</style>` 前的
   「Workspace 視覺改版 V1」區塊。
4. **移除首頁「今日教練」卡**（260719.2245）：與「今日教練任務」內容重複，使用者定奪保留
   時間軸 —— **待辦 4d 結案**。相關 CSS（mc-tc-*）一併清除。
5. **今日教練任務課卡與行事曆統一**（260719.2245，使用者指示）：改為行事曆定版語言 ——
   方形＋1px 細黑框、課程色淡底＋accent 字、第三行教練縮寫 tag、右上完成勾章。
   Calm Play 形狀 Token（clip-path）自首頁退場。
6. **導覽分流**（260719.2245，使用者指示）：管理員專屬內容（員工/出勤/財務總覽/營運分析/
   人資薪資勞健保特休工作規則/課程方案/動作資料庫/權限/系統設定）統一收進側欄「**管理員**」群組；
   排班獨立「**主管**」群組（`sup:true`，僅 `is_supervisor` 顯示、與管理員**疊加**不互斥）；
   櫃台僅四區（首頁總覽/預約/會員/票券・收款，靠既有 `fd` 旗標）。舊群組 key（g_staff/
   g_analytics/g_settings）由 `NAV2_GROUP_ALIAS` 在 `navTo` 入口對應，呼叫點不需改。
   **三種視角（管理員/櫃台/管理員兼主管）皆已瀏覽器實測**（櫃台/主管視角以 console 切
   SESSION 驗證 UI，僅前端驗證；RLS 級驗證仍需真帳號登入，見 §4）。
7. **決策補記**：櫃台**不開放**訓練紀錄（training_logs RLS 收斂維持）；簽到全面開放
   （會員自簽＋教練＋櫃台代簽，會員自簽尚未實作）。

8. **行事曆「營業前」列改回流內**（260719.2255，使用者指示）：原 `position:sticky;top:34px`
   捲動時黏在日期列下方；改為時間軌一般流內元素（黏著 08:30 前那格、隨格線捲動）。已實測。
10. **視覺改版 V2：頂部玻璃導覽**（260719.2320，使用者指示「導覽/設定/帳戶都在上方＋半透明感」）：
    左側深綠側欄改為**頂部玻璃導覽列**（純 CSS 重排，DOM/renderLeftSidebar 不動）——
    品牌左上、群組膠囊導覽置中（active＝品牌綠實色膠囊）、右上營運資訊＋帳戶 chip（選單改往下開）；
    mc-mode 卡片改半透明毛玻璃（backdrop-filter）。樣式在 `</style>` 前「Workspace 視覺改版 V2」
    區塊；平板 1080px 斷點一併蓋掉舊 64px rail。首頁與行事曆皆已截圖實測。
    註：body 的 `mc-collapsed` 收合機制原本就無對應 CSS（死功能），頂列化後更無作用，未動。
11. **行事曆課程篩選 chips**（260719.2330，使用者指示）：移除形狀 glyph、加回課程色
    （淡底＋accent 字與課卡同源；選中＝該課程 accent 實色白字）。`calCourseChips()`＋chip CSS。
12. **🐛 日曆切回週曆殘留單日起點**（260719.2340）：`setCalDays` 只有 n===5（5 天視窗時代遺留）
    與 n===1 分支，週曆改 7 天後切回不重對齊 → 第一欄變成剛剛日曆那天。補 n===7：對齊
    「目前檢視日」所在週的週一（留在同一週，不硬拉回本週）。已實測 日曆→週曆 回到週一。
13. **預約卡片文字層級**（260719.2350，使用者指示）：會員姓名放大（11.5→13px，7日檢視 10→11.5px）
    為卡片主角；時間與教練縮寫 tag 取消粗體（800→500）退居輔助。行事曆卡與首頁任務卡同步。
14. **狀態卡精簡**（260720.0005，使用者指示）：移除「今日營運正常」標題與營運摘要副標，
    只留四 KPI 並拿掉 KPI 的 onclick（純資訊展示）。整條狀態文案計算鏈
    （_statusText/_opsSummary/_todoTotal 等）一併清除。
15. **新增「今日值班」圈型進度卡**（260720.0020，使用者指示）：共用元件 `dutyRingHTML()` ——
    一人一環（已值時數／排班時數，無排班預設 6h）、中央時數、下方姓名＋上班–下班時間，
    值班中＝深綠環即時累計、已下班＝鼠尾草綠。管理員首頁（狀態卡下方，對象=當日有上班打卡
    的所有員工含櫃台）＋教練首頁今日摘要（已打卡才顯示個人環）。測試庫今日無打卡，
    以合成資料注入驗證渲染；真實資料驗證待有人打卡。
16. **首頁再迭代**（260720.0035~0045，使用者指示）：①KPI 改一張張獨立迷你卡（2×2）縮小佔位；
    ②「今日值班」卡移到 KPI 右側同列（`.mc-kpiduty` flex，窄容器自動換行）；
    ③值班時數改**每半小時記 0.5**（`dutyRingHTML` 對 workedMin 取 30 分下取整，環與數字同步）；
    ④今日教練任務課卡改**直式**（74×88）。皆已截圖實測。
17. **首頁三迭代**（260720.0055，使用者指示）：KPI 小卡也改直式（icon/數字/標籤直排）四張並排；
    教練任務卡放大（84×98，姓名 14.5px／時間 11.5px／tag 9.5px）。
18. **凌晨批次（260720.0115~0155，皆使用者指示、皆已截圖實測）**：
    ① KPI 移除值班教練/未打卡（與今日值班卡重複），新增「今日營收」（今日售票金額，
      拆新約/續約/分期：有 installment=分期，否則依會員是否首購分新約/續約）。
    ② 預約卡點選滑出四按鈕**擴及首頁教練任務卡**（onTcardClick 重用 expandBkCard 整套；
      .tcard-std 平行一份 orbit/roster CSS；:has(.cal-ev-active) 暫停清單裁切）。
    ③ **「編輯」改跳視窗**：bkOrbitEdit 改開預約明細 Modal（含編輯欄位＋儲存變更）；
      Phase 3b 卡內原地編輯（bkInlineEdit）退場、函式暫留。
    ④ 行事曆**最左卡點選不再右移**：_bkOrbitNudge 移除左緣推回，左側按鈕直接疊在時間欄上
      （展開卡 z-index 650 > 時間欄 25）。
    ⑤ 任務卡 hover 放大被裁切 → .tcard-list padding 6px + 負 margin 留呼吸空間。
    ⑥ **頂列重排**：移除右側「時間|營運正常|打卡」小字列（DOM 留存僅隱藏）；首頁大時鐘移入
      頂列右側（#mc-topclock，由全域 _clockTimer 每秒更新）；導覽膠囊改對整條頂列絕對置中
      （平板退回流內置中）。首頁 Hero 只剩問候＋日期。
    ⑦ **行事曆工具列三列→兩列**：移除「週曆/日曆」切換（日曆模式退場；手機窄幅自動單日
      機制保留）；「今天」併入中間日期導覽；「新增預約」移最右；教練 chips 上移第一列左側。
    ⑧ **三角色手機版預覽頁**：本機伺服器（scratchpad `serve8765.js`，Claude 會話工具，
      不進 repo）新增 `http://localhost:8765/__preview` —— 並排三個 390px iframe，
      載入後自動以前端視角模擬把第 2/3 格切成教練/會員（同源 eval 切 SESSION，僅版面預覽、
      共用管理員登入資料）。搭配另一分頁開桌機版即可雙視窗對照。
19. **深夜批次（260720.0210~0315，皆使用者指示、皆已截圖實測）**：
    ① **行事曆顯示到 22:00**：`CAL_END_H` 22→22.5（時間軸多 22:00 標籤與 22:00–22:30 格，
      22:00 開課完整可見；拖曳上限/現在線隨常數自動延伸）。§5 已知問題「22:00 預約看不見」結案。
    ② 手機版三角色第一個分頁統一命名「**首頁**」（ADMIN/COACH/MEMBER 底部導覽＋手機主導覽共 5 處）。
    ③ 手機版管理員今日營收卡爆版 → ≤900px KPI 兩欄、營收卡跨整列、細項可換行。
    ④ 手機版今日教練任務：移除課程色圖例；教練列改用**員工首頁同款圓形課卡**（task-dot，
      時間在圓內/姓名在下/完成實心，點圓開明細），值班進度條可並存。
    ⑤ 上方第一列統一：桌機/手機首頁皆只放**置中日期**（15px 不加粗，與行事曆標題同規格），
      問候語退場；行事曆標題同步取消粗體。
    ⑥ **翻頁動畫修復**：原 `_calAnimateSlide` 對舊 DOM 加 class 後隨即被重繪吃掉（等於沒動畫）；
      改 `window._calPendingSlide` 旗標、由 renderCalendar 直接輸出在新 `.cal-body` 上播放。
    ⑦ 行事曆日期導覽（含翻頁按鈕）真正置中：`.cal-head` 左右兩側各 flex:1 對稱。
20. **🔴 重大修復：簽到發點自 7/16 切 RPC 後從未對真實預約生效**（260720，葉政義簽到後追查發現）：
    - 根因：`handle_checkin_reward` 以 `bookings.benefit_type` 對 `reward_rules`，但除 7/17 兩筆
      測試預約外**全庫 benefit_type 皆 null**（匯入與所有建立路徑都沒寫）→ 一律 skipped。
      7/13~16 的 53 筆發點是切 RPC 前舊前端直發的；7/17 的 RPC 驗證用的是有 benefit_type 的
      測試預約，所以當時綠燈、真實會員課全漏。**教訓＝驗證要用真實資料的形狀，不能只用
      自己剛建立的測試資料。**
    - 影響：7/16~19 共 **41 筆**已簽到教練課漏發（各 2 點；效期依課程日+6 皆未過期，可補救）。
    - 修法（`docs/migrations/20260720_01_fix_checkin_reward_benefit_type.sql`）：
      ①函式對 null benefit_type 推導（私人教練＋票券名含「友善」→friendly_session，否則
      coaching_session；兩規則同為 2 點/7 天）；②補發該 41 筆（以 not exists grant 防重複）。
      **測試庫已套用並端到端驗證**（null 推導→發 2 點、效期=課程日+6 正確）。
      **✅ 正式庫已由使用者於 SQL Editor 執行完成（2026-07-20 清晨）並驗證**：
      7/16 起已簽到教練課全數 issued（56 筆、零 skipped）、當日共發 54 張票/108 點/52 位會員、
      效期 7/22~7/25 皆有效。葉政義 IMP-00221 已發 2 點（效期 7/24）。**本案結案。**
      註：SQL Editor 執行時最後顯示的多列空白＝逐筆呼叫 void 函式的正常輸出。
21. **手機版再迭代**（260720.0345~0400，使用者指示）：①移除教練（免打卡者的「掃描」中央鈕，
    打卡者不受影響）與會員（「簽到」中央鈕）的簽到入口——**新位置待使用者說明**，scan_checkin
    頁與處理程式保留；②管理員 KPI 兩卡並列：教練課＋團體課合併一格（大數字=總堂數、副標拆分）
    ＋今日營收卡。皆已於三角色預覽頁截圖實測。
    ③（260720.0415）管理員手機版今日教練任務改**每教練一列**：左欄「教練名（課堂數）」、
    右邊圓形預約卡（task-dot 46px 縮版）、**無互動**（原 mtc-card 折疊卡、值班進度條、
    展開時間軸整組退場，值班資訊由今日值班圈型卡承接；toggleMtcCard 暫存未刪）。
    ④（260720.0430）管理員手機版待辦新增兩列（手機限定）：**今日到期票券**（副標列會員名單，
    以測試資料驗證渲染）、**本月即將降級**（`tierDemotionWatchIds()`＝主顧客＋上一完整月<4 堂
    ＋本月至今<4 堂；7 月為起點月名單必空，8 月起生效）。
    ⑤（260720.0440）教練底部導覽「打卡」中央鈕也移除——下方導覽不放打卡/簽到入口
    （refreshPunchFab／punchFabTap／掃描頁程式全保留，新位置待使用者指定）。
    ⑥（260720.0455）**教練首頁改版**：第一列與管理員統一（置中日期、15px 不加粗）；
    第二列兩卡並列 —— 今日課程（總堂數＋副標教練課/團體課）＋**今日值班**（已打卡＝桌機同款
    圈型進度環 dutyRingHTML＋「下班打卡」鈕；未打卡＝排班時數＋「上班打卡」鈕；免打卡＝標示）。
    **打卡入口自底部導覽移入今日值班卡**（⑤的疑慮解除）。原「今日總覽」三格卡退場；
    區塊④ summaryCards 原本就未掛進版面（死碼，暫留）。
    ⑦（260720.0510）教練「本月成績」：**移除教練課環的 50/80/100 門檻刻度與達標列**
    （本頁所有聘僱類型共用，門檻獎金僅正職適用）；**新增值班時數環**（已完成/本月排定，
    金色）——有排班月顯示三環並列、無排班維持兩環放大版。已截圖實測（測試教練 12h/12h）。
    ⑧（260720.0520→0810 定版）管理員手機版待辦卡內容**只放兩列**：「**今日簽約名單**」
    （今日購票會員，與今日營收 KPI 同源）、「**本月即將降級名單**」（tierDemotionWatchIds）。
    無資料也顯示列（副標空狀態文字＋0），桌機維持原五項待辦。
    ⑨（260720.0540）教練「今日值班」卡四態定版：已打卡＝實圈＋下班鈕；未打卡（需打卡）＝
    **金色脈動相機提示鈕**＋排班時數＋上班打卡鈕；免打卡有排班＝**以班表推算的圈型進度**
    （班表起訖冒充打卡、班末定格）；免打卡無排班＝時數＋標示。
    ⑩（260720.0550）本月成績環副標改「已完成 X / 本月安排 Y 堂」「已值 X / 本月排定 Y 小時」；
    「本月出勤」卡改**滿版月曆**（左側統計欄退場，數字已併入環副標），每格＝日期＋當日
    課堂數合計（值班圈記/今日高亮保留）。
    ⑩b（260720.0615）本月成績環副標再簡化為純數字「3 / 8」「12 / 12」（不帶文字）。
    ⑱b（260720.0915→0930）月曆今日提示：整格填色退場、僅標日期 —— 定版為**實心綠圓白字**
    （下方班別/課數保持原色）。
    ㉒（260720.1010）①會員月曆**點日期不再展開**下方清單（格子改純顯示，選日清單移除）；
    ②管理員/教練首頁月曆**點日期＝切換下方「今日」各卡**（dashPickDate→_dashViewDate、
    coachPickDay2→_coachViewDate，keep 旗標保住選月；選取日 mc-sel 標記）；
    ③手機版預約行事曆（agenda）課卡改**桌機定版樣式**（細黑框＋課程色淡底＋accent 字、
    **無教練 tag**；其他教練/過去時段的淡化層級保留）。
    ㉑（260720.0955）**三角色首頁第一列統一為月曆**（置中日期列退場）：管理員手機版新增
    全店月曆（每日總課數、admCalShift 翻月；桌機維持日期列）；教練出勤月曆與會員月曆移至
    首列。三角色版式：月曆 → KPI 兩卡 → 任務 →（成績）。
    ⑳（260720.0940）手機版卡片標題統一：管理員 .mc-card-h（14px）拉齊員工/會員 .dcard-title
    的 text-section（20px）規格（@media≤600 提升，桌機不動）。
    ⑲（260720.0925）教練未打卡狀態定版：「今日值班」卡＝值班環同款**圓形空圈＋中央掃描圖示**，
    點圈開掃描打卡（金色脈動鈕與上班打卡按鈕退場）；已打卡＝進度環＋下班鈕、免打卡＝班表推算環。
    ⑱（260720.0905）教練/會員首頁月曆**固定格高**：教練每格三行（日期/班別代號/課數）、
    會員兩行（日期/課數圈），無資料以 visibility:hidden 佔位，避免某週突然變高。
    ⑰（260720.0845→0855）管理員手機版快速操作卡→「**本月成績**」（桌機維持快速操作）：
    教練課 done/sched＋團體課人次兩環；**營收環移除**；若管理員本月有排班則第三環顯示
    自己的值班時數（已值/排定）。教練首頁**本月成績移至最下**（出勤月曆之後）。
    ⑯（260720.0820~0830）教練出勤月曆值班標記改**班別代號**（金圈退場）：日期下第二行
    A早班(<12)/B中班(12-17)/C晚班(≥17)、第三行課堂數（無 start_time 舊資料顯示「值」）。
    **待辦卡兩列（今日簽約名單/本月即將降級名單）擴及桌機**——全平台統一，原五項待辦全退場
    （_pendingCheckin 等計算暫留未刪）。
    ⑮（260720.0800）管理員手機任務列：自己永遠第一列（手機專屬排序）；名稱固定欄 88→74px
    縮短與課卡距離；每位教練課卡列改**獨立橫向捲動**（nowrap＋overflow-x，課多左右滑）。
    ⑭（260720.0750）管理員底欄移除「其他」（剩首頁/行事曆/報表；員工/設定/登出由右上帳號
    選單承接）；教練「本月出勤」月曆去標題、改與會員月曆同款格式（mc-grid/mc-cell、週日起頭、
    mc-dot 課數；值班日金圈 .cattn-duty）。
    ⑬（260720.0710~0735）收尾三件：①教練/會員 KPI 卡補圖示（OPS_TODO_IC，與管理員一致）；
    ②教練頁 .dash-wrap 自帶 gap14 與統一卡距 12 疊成 26px → 手機版歸零 gap（段落間距統一 12px）；
    ③教練「本月出勤」月曆加「‹ YYYY/MM ›」單列切換（coachAttnShift，資料按檢視月重算）；
    ④**管理員手機版行事曆改用教練版 agenda 格式**（renderCoachAgenda，圖層預設全開＝全店課表；
    原滿版週曆 renderWeekTimeline 退場暫留）。皆已截圖實測。
    ⑫（260720.0655）**手機版品牌綠框架**：頂欄與底部導覽改品牌綠（logo/漢堡轉淺色、
    底欄未選淡白/選中純白）；三角色首頁段落節奏統一（卡片圓角 18、段落間距一律 12px，
    原 card 22px 與 dcard 各自為政的間距收斂）。@media≤600px 全站生效。
    ⑪b（260720.0620~0640）會員月曆：「我的課表」標題移除、月份切換改單列「‹ 2026/07 ›」
    （手機斷點原強制直排，inline flex-direction:row 蓋掉）；教練本月成績團體課環移除
    「平均 X 人/堂」（不發平均人數獎金）。
    ⑪（260720.0605）**會員首頁改版**：置中日期＋兩卡（已安排課程＝未來已預約堂數／
    未安排課程＝可用票券剩餘－已安排）＋「已安排任務」圓卡列（圓內時間、下方日期、
    今日實心未來空心、點開明細）＋我的課表月曆保留。原 SCHEDULE 大標與今日甘特圖退場
    （renderMemTodayGantt 暫留）。三角色首頁版式自此統一。
21. **上午批次（260720.1115~1130，皆使用者指示、皆三角色預覽實測）**：
    ① **鎖定 viewport 縮放**（使用者回報行事曆誤觸 pinch-zoom 後日期被 sticky 標題遮住）：
      `user-scalable=no`。真手機手勢待正式站確認。
    ② **月曆 Hero**（參考 Daily Task Schedule App 設計圖）：手機首頁月曆與品牌綠頂欄融為一體
      （負 margin 吃掉 .content 內距、綠底淺字、圓底角），三角色月曆改逐週列結構；
      往下捲 >70px 收合為「當週」一列釘在頂欄下、回頂展開（遲滯 8/70 防抖）。
    ③ **前後景層次**：月曆退為背景層（z-20），Hero 之後內容包進 `.hero-sheet` 前景片
      （圓上角＋上緣陰影＋負 margin 疊在月曆底緣上）；收合後當週列 z-60 抬回最上層。
      桌機 `.hero-sheet` 為 display:contents 透明包裝（管理員 .mc-dash-wrap 兼任
      container query 容器，排除在外）。
    ④ **短頁收合回彈循環根治**：原「內容不夠長就不收合」門檻移除，改為收合期間
      `.cal-hero-min~.hero-sheet` 補 min-height 保底可捲範圍——教練/會員短頁自此正常收合
      （內容一屏放得完、完全捲不動的頁面自然不收，屬合理行為）。
    ⑤ **教練底欄品牌綠修復**：`.role-coach .bottom-nav` 暖米底（兩層 class）權重高於
      手機品牌綠的 `.bottom-nav`（媒體查詢不加權重）→ 品牌綠選擇器並列補上同權重即蓋過。
    ⑥ 任務卡 hover 放大被 sticky 教練欄遮住 → hover 抬 z-index。
    ⑦（260720.1140）**月曆收合改三段式**（使用者指示：要有一段「上方縮小、下方內容含 KPI
      完整可見」）：展開不再由「捲回 <8px」自動觸發 —— 捲回頂端月曆保持收合；
      已在頂端後「再拉一次」才展開（觸控下拉 >18px／滾輪再往上，皆 passive listener）。
    ⑫（260720.1230）**員工資料清單簡化**（使用者確認頁面可正常操作；並定下
      **全系統原則：內容盡量簡化、圖示輔助閱讀**——之後每頁迭代都照此改）：
      原 8 欄表格（手機一人 7 行、大量「—」）→ 一人一列圖示卡（頭像＋姓名#編號＋
      店長/主管章＋💼類型＋📅到職年資＋狀態，空欄位不顯示，點列開詳情）。詳情表單未動。
    ⑬（260720.1240）**預設頭像改回基礎款**（使用者指示）：genderAvatarSVG 與員工/會員
      資料表（PP_ICON）一律用「頭＋肩」基本人形，健美男/女款退場（定義暫留，git 可回復）。
    ⑱（260720.1415）**課卡快捷鈕改圓形＋團課「新增」＋行事曆兩修**（使用者指示/回報）：
      - 周邊快捷鈕改**圓形 icon 鈕＋下方小字標籤**（EVO_IC 線條圖示：⇄代課/✕取消/✎編輯/
        ✓簽到/↩取消簽到/📄明細；evoBtn() 產生器，行事曆與首頁任務卡兩份 CSS 同步）；
        上排鈕標籤朝上生長防重疊；兩段式確認（取消/取消簽到）同樣圓形化。
      - **團課加第五顆「新增」**（頂上 evo-t1，＋icon）→ openGroupMembers 加會員進名單；
        首頁 .tl-panel overflow:hidden 會裁掉它 → :has(.cal-ev-active) 豁免（使用者實測回報）。
      - **今天欄底色只到 18:00 修復**：.cal-body flex stretch 把欄高鎖在可視高度、格子其實
        更長 → 欄背景/直向框線中途截斷；改 align-items:flex-start 讓欄高跟隨內容。
      - **友善課時段背景提示**：平日 10:00–18:00 格子加極淡友善藍（.fw-win），週末無。
      - 測試種子 BK-ORBIT-1 / BK-ORBIT-G（今日 15:00/17:00，測試庫）留給使用者續測，
        清除：delete from bookings where id like 'BK-ORBIT-%'。
    ⑰（260720.1345）**新增預約視窗改版**（使用者指示，皆桌機實測）：
      ①hover 慢半拍根因＝.modal-bg 全螢幕 backdrop-filter:blur → 移除（遮罩加深補對比），
        .bk-card transition 改只動 border/background/shadow —— 全站 Modal 都受惠；
      ②日期/時間移到視窗最上方並排；
      ③課程排序固定：第一列 教練課/友善教練課/團體課、第二列 運動按摩/體驗課/自主訓練；
      ④教練名單開窗即顯示（不再等選完課程才出現）；
      ⑤步驟 2 上方加白底摘要卡（課種 glyph×課程色＋LNI 線條圖示：日期(週幾)/時間/教練），
        三種變體（一般/團體/體驗）共用 bkSummaryCard；課種判定抽成 bkCcOfType 共用。
    ⑯（260720.1330）**點欄即編輯＋預約 LAG 對策**（使用者回饋「要按編輯才能改不直覺」
      「預約課程總是 LAG」）：
      - **通則定案：簡單欄位點了就能改，「編輯」只留給進階/大量欄位。**
        人物資料表（PP sheet，會員/員工共用）：檢視模式點任一可編輯欄位→直接進編輯並聚焦
        該欄（ppEditFocus；唯讀/系統/自動計算欄除外；hover 顯示 ✎）；表頭電話/職稱/到職日
        同樣可點。會員詳情（mdetail）原本 Email/生日/LINE/主教練/標籤已可點，補「姓名」
        （openFieldEdit 加 name、存檔擋空白）。
      - **LAG 根因**：每個 Supabase 往返約 0.5s；簽到/取消/建立後 dbCacheClear() 全清
        → 重抓 5~8 張表＝1~3 秒；大表分頁又是逐頁序抓。三刀：
        ①三個 RPC 呼叫點改只清動到的表（bookings/member_tickets/ticket_logs/notifications）；
        ②dbGetAll 分頁改波次並行（第 1 頁後一次並行抓 3 頁）；
        ③少變動表 TTL 拉長（ticket_types/course_plans 60s、exercises 300s——只影響
        「別台裝置改動」的可見延遲，本機寫入仍即時清）。
        桌機測試庫驗證行事曆/會員詳情/員工表正常；**正式庫大資料的體感改善待真機確認**。
    ⑮（260720.1305）**預約介面檢查＋圖示風格定調**（使用者指示「圖示保持簡單線條」）：
      新增全域 **LNI 線條圖示集**（stroke SVG、.lni 15px 行內；搜 `const LNI`），
      今日新增的 emoji 全數替換（員工清單/下班確認/票券屬性/營業時間）。
      新增預約視窗檢查：兩步流程本身乾淨；步驟 1 課程卡加課種形狀 glyph（■●▲◆⬢×課程色，
      color 欄位空時由類別推導、名稱含「友善」歸友善），副標與名稱重複時不顯示。
      **慣例：圖示一律線條 SVG，不用彩色 emoji。**
    ⑭（260720.1250）**桌機版囉嗦內容掃描與簡化**（使用者指示；逐頁截圖檢查）：
      掃過 首頁/會員管理/員工管理/課程方案/系統設定/權限設定，改了三處——
      ①系統設定「場地分配規則」一段文字牆 → 課種 glyph 條列（■▲/◆/⬢ 各一行＋一行補註）；
      ②「營業時間」每週 7 列表格（週一~五重複）→ 相同時段連續天合併成 3 行（🕘）；
      ③課程方案「票券種類」變體/會員可自約/需指定教練/限時段 4 個稀疏欄 → 併成
      「屬性」一欄，只顯示為真的圖示 chip。其餘頁大致乾淨；候選後續：員工管理
      職稱/工作規則兩空欄、票券模板表 9 欄。
      註：navTo('plans') 是子渲染器（渲染進 #tk-body），直接導頁會白畫面＋重複包 subnav
      —— UI 沒有這種入口，僅開發時注意。
    ⑩（260720.1215）**會員自行簽到上線**（使用者定案：簽到放首頁「已安排任務」課卡、
      開課前 30 分鐘開放）：任務圓卡新狀態＝可簽到（金環＋金字）/已簽到（勾章、卡片不消失）；
      點卡開預約明細，會員視角＝唯讀（無編輯欄位、姓名純文字）＋「簽到」鈕（未達 30 分鐘窗
      顯示「開課前 30 分鐘開放簽到」）。三層閘：按鈕顯示、checkInBooking 既有 30 分鐘閘、
      RPC fn_checkin_booking(member_app) 驗本人（後端早已支援，正式庫已套）。
      預覽以測試庫種子（BK-SELFCK-*，已清）跑通全流程含發點。
      註：30 分鐘窗僅前端強制，RPC 端未擋「提早簽到」——與 §4「RPC 未檢查到期日」同類，
      待補伺服器端檢查。**待辦：真會員帳號登入的 RPC 路徑實測**（預覽共用管理員 auth，
      member_app 分支走的是回退路徑）。
    ⑪（260720.1215）**教練下班打卡確認視窗升級**（使用者指示）：圖示統計列（上班/現在/
      今日值班＋排班對照）；比排班早走＝「⚠️ 時間不足」警示（排班到幾點、還差幾小時，
      0.5h 級距）＋紅色「確定提早下班」；達標＝綠色「已完成今日排班時數」。以假資料實測兩態。
    ⑨（260720.1200）三件：**小管家面板與漢堡選單互斥**（開一個自動關另一個）；
      **小知識庫 20→40 則**（新增 營養/有氧/習慣 三類；組合數 8 運勢 × 40 = 320）；
      **收合後月曆維持背景層**（使用者指示：內容片永遠在前 —— cal-hero-min 不再抬 z）。
      註：背景分頁會凍結 rAF/scroll 事件，收合在背景 iframe 不觸發是瀏覽器行為、非 bug
      （除錯時記得把預覽分頁切到前景）。
    ⑧（260720.1150）**每日抽籤移入左上小管家面板**（使用者指示：漢堡抽屜高度不夠顯示）：
      小燈泡彈出面板（tb-butler-pop）加「每日抽籤/小知識」鈕＋內嵌結果（id tb-fortune-pop，
      renderFortuneInline/drawFortuneInline 改吃 boxId 參數）；面板加 max-height 可捲動。
      手機版隱藏抽屜入口（.tb-acct-fortune/#tb-fortune-inline），桌機保留
      （mc-mode 隱藏小管家、仍需抽屜入口）。開面板時當天已抽直接顯示結果。
9. ✅ **正式庫已套用 `20260719_01`（發點新規則）**：MCP 寫入被權限分類器擋，改由使用者在
   SQL Editor 手動執行成功；已驗證函式雜湊與測試庫一致（711e2fdc…）、anon/authenticated
   EXECUTE 已收回。**補簽也發點自此正式生效。**

**新增待辦（接上 §4）**：
- **葉政義 IMP-00221 補簽**：規則已生效，請使用者在正式站行事曆點該卡「簽到」即可
  （會發 2 點、效期至 7/24；由使用者操作以正確記錄操作者）。
- 測試庫員工邀請流程已被 migration 03 關閉（等安全 RPC 版）。
- 會員自行簽到（新決策）＋會員註冊前端（DB 已就位）＝下一波主要工程。
- 視覺 V1＋導覽分流僅本機未 commit/未推；待使用者過目後上線。

## 1.5 昨天（2026-07-17）做了什麼

**首頁改版 Dashboard V2**（依使用者「首頁資訊更新」設計稿）：Hero 只留問候＋時鐘、狀態卡帶 4 個 KPI、
右欄「今日待辦事項」＋「快速操作 2×2」、教練任務列不再捲動（有幾位長多少）。移除今日營收 KPI、
課堂類型圓餅圖、今日值班卡（皆經使用者確認）。窄視窗以 @container 換行、右欄撐滿。清掉 −120 行死碼。
Slime 10.3 收尾（Hero／工作中心／提醒改用 .slime-flow）。

**一日流程測試發現並修掉的 bug（皆已上線、皆在測試庫或正式庫規模驗證過）：**
1. **教練課可以沒有教練** —— 從「新增預約」建教練課，教練欄不出現、送出建立 coach_id=null 的私人
   教練課、票照扣。根因：needCoach 判斷依賴後台可設的 requires_coach 旗標，而「VIP 教練課」「限定
   教練課」該旗標為 false。改以 category 為準（bkNeedCoach()）。正式庫「限定教練課」尚有 10 堂可用，
   是活的風險 —— 程式已修，但這兩筆資料的 requires_coach 建議一併改為 true（見 §4）。
2. **手機號撞號 + 無格式驗證** —— 建會員時手機未 normPhone、無格式驗證，'abc' 也能建；且
   0912345678 與 0912-345-678 重複檢查測不到卻登入時撞同一帳號。已在存檔前正規化＋驗 ^09\d{8}$。
3. **票券到期日 null 顯示成字串 "null"** —— 正式庫 2,229 筆有 2,073 筆無到期日（舊方案「永久有效」）。
   新增 fmtExpire()，統一顯示「永久有效」。
4. **切頁 LAG** —— dbGetAll 無快取，refreshNavBadges 每次切頁重複撈 bookings/purchase_applications。
   加 8 秒 TTL 快取＋寫入即失效（含三條 RPC）。切頁網路請求 8→6、後續切頁 0。
5. **錯誤訊息露出 SQL 原文** —— 教練打卡失敗看到「new row violates row-level security policy...」。
   資料層統一翻譯（dbFriendlyError）＋表名對照＋showToast 可設時長。這正是 7/16「教練說有打卡」的成因。

**票券頁 UX**：會員票券列表加搜尋／狀態篩選／限筆 100；發放票券的 432 選項 <select> 加即時搜尋；
快速操作「售票/儲值」直接落在「會員票券」分頁（原本落在空的購買申請分頁）。

**行事曆流程驗證通過**（取消退票、簽到發點）：24h 外取消退 1 堂、24h 內不退；教練課簽到發自主訓練
2 點、效期 7 天自課程日起算 —— 完全符合票券規格 V4。ticket_logs 帳本 deduct/refund/grant 皆成對正確。

## 2. 正式環境

- GitHub：`TYehYu/yugym-booking-system-app`（公開，主分支 `master`）。**推 master = 上線**（GitHub Pages，1~2 分鐘生效）。
- 正式網址：https://tyehyu.github.io/yugym-booking-system-app/
- Supabase 正式：`rlpiomzplckzqnqrvrwc`（含真實會員資料，**勿搬個資到本機/測試**）。
- Supabase 測試：`kucvxpjatptfckhlxptj`（`yugym-sprint3-test`，免費，schema 與正式 1:1）。
  **已決定長期留用**（靠它重現問題、驗證修補）。帳密見上述 PRIVATE 檔。
  測試時灌大量假資料驗證正式庫規模，一律用可辨識前綴（`BK-SEED17-*`/`m-BULK17-*`/`TK-BULK17-*`）
  方便一鍵清除；今天所有測試資料已清乾淨，僅保留 `BK-SEED17-*`（18 筆今日課）與 `SH-SEED17-*`
  （3 筆排班）供下次示範首頁用，不需要可 `delete from bookings where id like 'BK-SEED17-%'` 清除。
- 前端旗標（`window.FEATURE_FLAGS`）**全開**：`createBookingRpc / cancelBookingRpc / checkinBookingRpc`。
- ⚠️ **本機 `config.js` 目前指向測試庫**，並以 `git update-index --skip-worktree config.js` 防止誤推
  （線上那份仍正確指向正式庫）。要改回正式庫：檔內註解留有原始設定；解除保護用
  `git update-index --no-skip-worktree config.js`。
- ⚠️ **快取層（commit 4d3f5e7）動的是全系統唯一資料入口 dbGetAll，影響每一頁**。若上線後出現
  「資料沒更新／看到舊資料」，第一個懷疑它，可 `git revert 4d3f5e7` 快速回退。日後新增 RPC 或
  直接 sb.from().insert/update/delete 時，務必比照加 dbCacheClear()。

## 2. 正式環境

- GitHub：`TYehYu/yugym-booking-system-app`（公開，主分支 `master`）。**推 master = 上線**（GitHub Pages，1~2 分鐘生效）。
- 正式網址：https://tyehyu.github.io/yugym-booking-system-app/
- Supabase 正式：`rlpiomzplckzqnqrvrwc`（含真實會員資料，**勿搬個資到本機/測試**）。
- Supabase 測試：`kucvxpjatptfckhlxptj`（`yugym-sprint3-test`，免費，schema 與正式 1:1）。
  **已決定長期留用**（當晚兩次靠它重現問題、驗證修補）。帳密見上述 PRIVATE 檔。
- 前端旗標（`window.FEATURE_FLAGS`）**全開**：`createBookingRpc / cancelBookingRpc / checkinBookingRpc`。
- ⚠️ **本機 `config.js` 目前指向測試庫**，並以 `git update-index --skip-worktree config.js` 防止誤推
  （線上那份仍正確指向正式庫）。要改回正式庫：檔內註解留有原始設定；解除保護用
  `git update-index --no-skip-worktree config.js`。

## 3. 已完成 / 進度

### Sprint 1~3（2026-07-16 白天～晚間）

- ✅ Sprint 1 盤點、2.5 票券對應：`docs/SPRINT1_SYSTEM_AUDIT.md`、`docs/SPRINT2_5_BENEFIT_MAPPING_AUDIT.md`
- ✅ Sprint 2 RLS 收斂：設計 `docs/SPRINT2_RLS_DESIGN.md`、`docs/SECURITY_MODEL.md`；測試 `docs/SPRINT2_RLS_TEST_RESULTS.md`；已套正式（`docs/migrations/20260716_03_rls_hardening.sql`）。未登入讀 members/tickets 已為 0。
- ✅ Sprint 3 RPC 切換：`docs/SPRINT3_RPC_MIGRATION_PLAN.md`、`docs/SPRINT3_RPC_TEST_RESULTS.md`、`docs/SPRINT3_FRONTEND_STATUS.md`；正式已套 `20260716_01_*.sql`、`_02_*.sql`。
- ✅ Baseline schema（補回 migration 歷史）：`docs/migrations/0000_*.sql`、`0001_*.sql`

### 當晚追加（2026-07-16 20:00 後）

- ✅ **RPC 授權收斂（已套正式）**：`docs/migrations/20260716_04_rpc_authz_hardening.sql`
  - 背景：`fn_*` / `benefit_*` 皆為 SECURITY DEFINER（設計上繞過 RLS），但 EXECUTE 沿用 PostgreSQL 預設值（授予 PUBLIC），且身分檢查不一致（僅 `fn_checkin_booking` 有做）。Sprint 2 的 RLS 只約束直接讀寫資料表，不涵蓋此路徑。
  - 修法：內部函式收回外部 EXECUTE；`fn_create_booking`/`fn_cancel_booking` 補身分檢查；三支前端 RPC 收回 anon。RLS 政策依賴的 7 支輔助函式維持原狀。詳見 migration 檔內說明與**日後新增 SECURITY DEFINER 函式的檢查清單**。
  - 已驗證：測試庫未登入呼叫全擋（42501）、員工三流程正常（扣退票正確）、會員越權回 `AUTH.FORBIDDEN`；正式庫已比對授權矩陣。
  - 查核：正式庫 `ticket_logs` 全數為正常人員操作，**無遭利用痕跡**。
- ✅ **教練無法下班打卡修復（已套正式）**：`docs/migrations/20260716_05_fix_attendance_self_write.sql`
  - Sprint 2 RLS 收斂將 `attendance` 的 `att_self` 寫成僅 `for select`，教練對自己的出勤只能讀不能寫，`punchOut()` 的 upsert 被擋。同份 migration 的 `punch_requests` 用相同條件卻給 `for all`，判定為漏寫。已比照改為 `for all`，範圍仍限縮 `emp_id = current_employee_id()`。
  - **使用者已實測：教練下班打卡正常。**
- ✅ **清除舊版（Codex 時代）遺留**：刪除 24 個檔案、15,113 行（`js/app.js`、`css/app.css`、`assets/` 舊品牌圖、重複的啟動檔、5 月份文件 16 份）。`README.md` 改寫為現況；`CLAUDE.md` 修正「`docs/` 為舊版遺留」的錯誤描述。
- ✅ **首頁總覽改版**：KPI 由 5 格併為 3 格（今日課堂含已完成/未完成明細）、消除卡片下方空白（stretch + 內容靠上所致）、教練任務只列今天有課者、圓餅圖圖例不再斷行。
- ✅ **Slime Layout Engine Phase 10.2a**：建立 `.slime-flow` / `.slime-card` / `.slime-stack` CSS 原語（見 `index.html` 內詳細註解）。採 flex-wrap 而非 grid auto-fit，因規格要求逐卡宣告 min/max/grow/shrink。
- ✅ **Slime Phase 10.3（試點）**：`.mc-main-row`（教練任務 + 值班/圓餅）改用 `.slime-flow`，移除 1100px 視窗斷點。順帶修掉「側邊欄於 1080px 隱藏、容器變寬 200px 但舊斷點不知情仍堆疊」的問題。**使用者已確認並排正常。**
- ✅ **平板側邊欄收成圖示列**：601~1080px 原本改放改版前的橫向 `.navbar-row`（等於平板維護第二套導覽、視覺回到舊設計）。改為同一個側邊欄收窄成 64px rail。

## 4. 待辦

### 🔴 等使用者決策（都是正式庫資料異動，我未擅自執行）

1. ~~**「VIP 教練課」「限定教練課」的資料歸屬**~~ **✅ 已結（2026-07-18）**：使用者澄清這兩者
   **就是一般教練課，只是當初用活動價格另立票種，且皆已停售**（`sellable=false`、`is_legacy=true`、
   `category=私人教練`、`benefit_type=coaching_session`）。故**無資料模型遷移需求** —— 沒有獨立的
   「VIP/限定方案」概念要搬進 `course_plans`，就是兩筆退役的教練課票種，67 張既有票（VIP 46、限定 21）
   照常使用即可。唯一筆誤 `requires_coach=false` 已於 2026-07-18 補為 `true`（正式庫，經使用者同意執行；
   回退＝改回 false）。預約行為昨天已由 `bkNeedCoach()` 看 category 修正，此次僅補齊後台顯示一致。
2. **1,016 堂「永久有效」票券**：正式庫 2,073 筆無到期日票券（250 筆仍有剩餘、共 1,016 堂、涉 412 位
   會員）。使用者已確認**這是舊方案的賣法（以前賣永久有效）**，非資料缺漏 —— 顯示已改為「永久有效」。
   **無需補資料**；但若日後要讓舊票也有效期，這批是對象。

### 立即

3. **正式站抽驗 Dashboard V2**：管理員登入正式站 → 首頁確認狀態卡 4 KPI、教練任務列、待辦、快速操作
   正常；各頁（預約/會員/財務/營運分析）不因改版而異常。（今天在測試庫已充分驗證，正式站僅需目視。）
4. **7/16 鄭百益的 `clock_out` 為 null**：仍未補（黃美蓉、曹智詠已於 7/16 補齊）。**由教練自行補打卡
   或送補卡申請** —— 出勤紀錄不由系統代填。今天已修「打卡失敗訊息看不懂」的根因（§1.5 第 5 點），
   日後再發生會顯示人話＋引導「請改用申請補打卡」。

### 接續開發

4b. **會員註冊帳號＋舊會員資料串聯**（2026-07-18 使用者指示）：讓會員自行註冊登入帳號、
    以手機號自動串聯既有會員資料。程式內已有半套（`memberRegisterLink()` 產 `#member-signup`
    申辦連結；後台建會員「不建帳號、之後可綁定」）。需先追完現況再設計：Auth 流程、
    重複手機處理、啟用預留的會員自我隔離 RLS。**動工前先給方案與風險清單。**
4c. **手機版首頁（三角色）**：規劃見使用者提供之《手機版首頁更新計畫 V1》——會員大 Token 首頁、
    教練值班+下一堂課、管理員 Today's Operations；全部沿用 Calm Play Shape Token 元件。
4d. **首頁時間軸去留**：「今日教練」卡與「今日教練任務」時間軸內容重疊，待使用者決定拿掉哪個。
4e. **reduced-motion 折衷**：使用者 Windows 關閉動畫效果，目前 hover 放大/微彈依無障礙慣例不播；
    是否改為「減少動態仍保留輕量縮放」待使用者決定。

5. **票券系統 V4**（規格已存 `docs/TICKET_SYSTEM_V4_SPEC.md`，尚未實作）：方案（Plan）與會員票券
   （快照）分離、後台 CRUD／停用／封存、分期規則。動工前先做文末的現況落差分析。上述待辦 #1 併入此。
6. **RPC 未檢查票券到期日**：`fn_create_booking/checkin/cancel` 定義中無 expire 字樣，到期檢查只在
   前端。永久有效票不受影響，但這與 7/16 的授權漏洞同類（前端擋 ≠ 系統擋）。若日後票券恢復有效期，
   應在 `fn_create_booking` 補到期檢查。
7. **Slime Phase 10.2b（FLIP 平滑動畫）／10.5（其餘管理頁）**：10.2b 只在拖曳視窗大小時看得到，
   投報率最低、風險最高，建議最後做。**10.4（行事曆）已評估為不適用**——行事曆日欄要壓縮不換行，
   套 slime 會破壞功能；當時另發現並修掉一個 `window.innerWidth` 估算欄寬的舊 bug（見下）。
8. **Dashboard V1.1 微調剩餘項**（改版後多數已被 V2 取代，重評估）：手機版教練任務改橫向滑動、
   提醒事項優先級 Badge（V2 待辦卡已用顏色分級，可視為部分達成）。
9. （未來）會員登入上線時，啟用已預留的會員自我隔離 RLS 政策。

## 5. 已知問題（評估後暫不處理）

- **行事曆課卡字級分級用 `window.innerWidth` 估算欄寬**（`index.html` 約 8299 行）：
  `colW = (window.innerWidth - 80) / nDays` 用視窗寬冒充容器寬，未扣側邊欄（232/64px）與 content
  padding，高估約 24%（1400px 視窗、7 日檢視：估 189px vs 實際 152px）。後果：該縮字的課卡沒縮，
  同時段兩張卡並排時字會擠出來。且渲染時只算一次、resize 不重算。正解是量測 `.cal-daycol` 實際
  `getBoundingClientRect().width` 或改 CSS container query。**今天探查 Slime 10.4 時發現，尚未修**
  （非新問題，一直存在；影響是美觀而非功能，故未擋上線）。

- **22:00 起始的預約在行事曆上看不見**：格線為 `mm < CAL_END_H*60`，最後一格 21:30，22:00 的課落在可視範圍外。
  早上側有 Phase B 動態展開處理（有早課自動往前長），**結束時間卻寫死**，無對稱機制。
  正式庫現況：全 1,449 筆有效預約中僅 **2 筆**（`IMP-01224` 5/28、`IMP-01200` 5/29），皆為 5 月匯入的舊資料、皆已完成。
  無營運影響（課已上完、票已扣），統計不受影響（直接讀 DB）；唯一症狀是翻回那兩週看不到那兩堂。
  修它需做整套動態結束展開（改 6 處寫死的 `CAL_END_H`，牽動拖曳上限與現在線），為 2 筆舊資料不划算。
  **若日後要開放 22:00 後的晚間課程，再一併處理**（動態展開 + `＋` 入口 + `bkTimeOptions` 延長至 23:00）。
  註：預約表單 `bkTimeOptions` 目前允許 08:00~22:00，故 22:00 的課「建得出來卻看不見」。

## 6. 回退方法（都很快）

- 某 RPC 流程出問題 → `index.html` 對應旗標改回 `false`、commit、push → ~30 秒生效。
- 前端改版有問題 → `git revert <commit>` 後 push → 1~2 分鐘生效。
- 某頁因 RLS 讀不到資料 → 對該表補開放政策：`create policy <t>_all on <t> for all using (true) with check (true);`（Supabase SQL Editor 或 MCP 執行，秒生效）。
- 資料庫 RPC / 政策若需還原 → 各 migration 檔內附回退語法。

## 7. 在新電腦接手的步驟

1. 安裝並登入 Claude Code（用你的 claude.ai 帳號；Supabase MCP 會隨帳號連上）。
2. `git clone https://github.com/TYehYu/yugym-booking-system-app.git`
3. 設定 git 身分：`git config --global user.name "..."`、`git config --global user.email "..."`
4. 若要用 gh CLI：`gh auth login`（帳號 TYehYu）。**注意**：Windows 上中文路徑會被編碼弄壞，腳本請放純英文路徑；輸入 `!` 指令前先按 Shift 切英文輸入法，否則會打成全形 `！` 而無反應。
5. 開新的 Claude Code 對話時，請它先讀 `CLAUDE.md` 與本 `docs/HANDOFF.md`。
6. **資安鑑識紀錄與測試帳密不在 repo 內** —— 在原機器的 `PRIVATE_DO_NOT_UPLOAD_security-incident-20260716.md`，換機器需另行複製（勿透過公開管道傳送）。
7. 本機預覽：點兩下 `開啟預約系統.bat`（埠 8765），或 `python -m http.server`。
   **`config.js` 決定連哪個庫，本機那份目前指向測試庫**（見 §2）。

## 8. 重要慣例（同 CLAUDE.md）

- 資料存取只走 `dbGetAll/dbGet/dbPut/dbDel`；`coaches` 別名對應 `employees`。
- `dbGetAll` 的 `.range()` 分頁不可拆（否則大表被截斷）。
- 改版更新 `APP_VERSION` / `APP_VERSION_LABEL`（格式 `YYMMDD.HHmm`）。
- 不搬真實會員個資到測試環境或本機。
- **新增 `SECURITY DEFINER` 函式時**：務必明確 `REVOKE EXECUTE FROM PUBLIC, anon`（勿依賴預設值）、只 GRANT 給實際需要的角色、函式內以 `is_any_staff()` / `current_member_id()` 檢查呼叫者身分。**RLS 綠燈不代表此路徑安全**，兩者需分別驗證。
- **GitHub Pages 快取 `Cache-Control: max-age=600`**：推版後看到舊版屬正常，按 `Ctrl+Shift+R` 強制重新整理，或等 10 分鐘。
