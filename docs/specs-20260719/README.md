# 2026-07-19 GPT 交接包（規格文件）· 使用前必讀

這批文件來自使用者 2026-07-19 與 ChatGPT 的規劃討論（原 ZIP：`YUGYM_Claude_Handoff_20260719.zip`），
內容為**營運規則與開發規範定案**，很有價值；但其中的「實作狀態」敘述與本 repo 實況有出入，
以下勘誤經 2026-07-19 逐項比對程式碼與兩個 Supabase 專案驗證。

## 勘誤（以本檔為準）

### 資料庫實況

- 三支 migration（已歸檔至 `docs/migrations/20260719_01~03_*.sql`）**已套用於測試庫**
  `kucvxpjatptfckhlxptj`（member_link_requests、兩支註冊 RPC、新版 handle_checkin_reward、
  training_logs/employees 政策收斂皆已驗證存在）。**正式庫完全未動**。
- `ENVIRONMENT_SYNC_RUNBOOK.md` 中「目前測試專案連線權限不足、不可宣稱已同步」為過時敘述。

### 「已完成」清單（規範與開發交接.md §11）的前端項目——repo 內不存在

品牌綠首頁、`?env=test` 環境切換與 TEST 標示、櫃台四入口導覽、今日會員簽到工作台、
管理員/櫃台/主管導覽分流——這些**都沒有進到本 repo**（git 歷史可證）。
另「主管排班／主管與店長獎金」是系統**既有功能**（`is_supervisor`/`is_manager`），非本批新做。

### 測試庫已知副作用（migration 03 造成，待處理）

1. **員工邀請流程失效**：`employees_insert`/`employees_invite_*` 政策被移除，
   現行前端 invite_token 邀請流程在測試庫不可用（正式庫不受影響）。
2. **櫃台看不到訓練紀錄**：`training_logs` 新政策僅開放管理員／該教練本人／會員本人，
   前端會員詳情等頁櫃台登入時會默默變空白。
   **已定案（2026-07-19 使用者）：櫃台不開放訓練紀錄，維持此收斂。**

## 使用者三項定案（2026-07-19，凌駕文件中矛盾處）

1. **視覺方向**：以 Crextio 參考圖（`下載.jpg`）為基準——柔和漸層工作畫布＋白色大圓角卡＋
   深色操作卡，**參考圖淡黃色系全數改為品牌綠**；金色僅留少量提醒。
   行事曆課卡維持 7/18 定版（方形細黑框），不受此波改動。
2. **簽到發點規則**：**補簽也發點**（依原課程日計效期；課程日為第 1 天、第 7 天到期）——
   即 `20260719_01` 的新版行為。正式庫尚未套用，套用需另行批准。
3. **簽到主體**：**全面開放**——會員可自行簽到、教練可代簽、櫃台可代簽。
   目的：保障會員簽到後取得自主訓練點數的權益。
   （原文件 §13「會員自行掃碼、教練不簽」與 UAT-04 的矛盾以此定案為準；UAT 清單引用時需修正。）
